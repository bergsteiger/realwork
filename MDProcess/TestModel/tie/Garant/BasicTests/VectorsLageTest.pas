unit VectorsLageTest;

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Библиотека "BasicTests"
// Модуль: "w:/MDProcess/TestModel/tie/Garant/BasicTests/VectorsLageTest.pas"
// Родные Delphi интерфейсы (.pas)
// Generated from UML model, root element: <<UtilityPack::Class>> TestModel::BasicTests::PrimitiveContainers::VectorsLageTest
//
//
// Все права принадлежат ООО НПП "Гарант-Сервис".
//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// ! Полностью генерируется с модели. Править руками - нельзя. !

interface

type
 BoolAgrVector = array of Boolean;

 CharRefAllocator = record
 end;//CharRefAllocator

 ShortRefAllocator = record
 end;//ShortRefAllocator

 DubleLnkAllocator = record
 end;//DubleLnkAllocator

 LongMultyAllocator = record
 end;//LongMultyAllocator

 LongRefVector = array of LongMultyAllocator;

 LongLnkVector = array of LongMultyAllocator;

 CharAgrVector = array of char;

 ShortRefVector = array of ShortRefAllocator;

 DoubleLnkVector = array of DubleLnkAllocator;

 LongAgrVector = array of LongMultyAllocator;

 MultyAllocator = record
 end;//MultyAllocator

 AStrAgrVector = array of MultyAllocator;

 AStrRefVector = array of MultyAllocator;

 AStrLnkVector = array of MultyAllocator;

 StrLnkVector = array of MultyAllocator;

 StrRefVector = array of MultyAllocator;

 StrAgrVector = array of MultyAllocator;

 PointerAgrVector = array of MultyAllocator;

 PointerRefVector = array of MultyAllocator;

 PointerLnkVector = array of MultyAllocator;

 IdentifierAgrVector = array of MultyAllocator;

 IdentifierRefVector = array of MultyAllocator;

 IdentifierLnkVector = array of MultyAllocator;

 ObjectRefVector = array of MultyAllocator;

 ObjectLnkVector = array of MultyAllocator;

 ExceptionAgrVector = array of MultyAllocator;

 ExceptionRefVector = array of MultyAllocator;

 ExceptionLnkVector = array of MultyAllocator;

 SizeMultyAllocator = record
 end;//SizeMultyAllocator

 SizeAgrVector = array of size;

 SizeRefVector = array of SizeMultyAllocator;

 SizeLnkVector = array of SizeMultyAllocator;

 ConstSizeAgrVector = array of SizeMultyAllocator;

 ConstSizeRefVector = array of SizeMultyAllocator;

 ConstSizeLnkVector = array of SizeMultyAllocator;

implementation

end.