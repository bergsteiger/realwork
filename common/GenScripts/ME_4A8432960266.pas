unit ctTypes;

interface

uses
 l3IntfUses
;

type
 TComboStyle = (
 );//TComboStyle
 
 TCurrentMode = (
 );//TCurrentMode
 
 TAutoWidthMode = (
 );//TAutoWidthMode
 
implementation

uses
 l3ImplUses
;

end.
