# Compilers
dir.delphi= Delphi path
dir.kylix= Kylix path
delphi.version7= Delphi 7 flag (on or off)

# Location
dir.bpl= Bpl directory

# dont use MTDORB_UCUtils - link it into exec
build.utillibrary=on

# External libraries
dir.lib.jcl= JCL library path (not required)
dir.lib.dunit= DUnit library path (required for some example)

# Build
build.debug= Debug build (on or off)
