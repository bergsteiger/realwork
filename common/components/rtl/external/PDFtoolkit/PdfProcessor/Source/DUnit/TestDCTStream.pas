unit TestDCTStream;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, Classes, Stream, DCTStream, FilterStream;

type
  // Test methods for class TgtDCTStream
  
  TestTgtDCTStream = class(TTestCase)
  strict private
    FgtDCTStream: TgtDCTStream;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  end;

implementation

procedure TestTgtDCTStream.SetUp;
begin
  FgtDCTStream := TgtDCTStream.Create(nil, False, 0);
end;

procedure TestTgtDCTStream.TearDown;
begin
  FgtDCTStream.Free;
  FgtDCTStream := nil;
end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTgtDCTStream.Suite);
end.

