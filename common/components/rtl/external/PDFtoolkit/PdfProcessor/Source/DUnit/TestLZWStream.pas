unit TestLZWStream;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, LZWStream, Stream, LZWEncode, FilterStream, Classes, LZWDecode;

type
  // Test methods for class TgtLZWStream
  
  TestTgtLZWStream = class(TTestCase)
  strict private
    FgtLZWStream: TgtLZWStream;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  end;

implementation

procedure TestTgtLZWStream.SetUp;
begin
  FgtLZWStream := TgtLZWStream.Create(nil, False, 0, 0, 0, 0, 0);
end;

procedure TestTgtLZWStream.TearDown;
begin
  FgtLZWStream.Free;
  FgtLZWStream := nil;
end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTgtLZWStream.Suite);
end.

