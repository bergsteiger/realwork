unit TestPredictor;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, Stream, Predictor, FilterStream;

type
  // Test methods for class TgtStreamPredictor
  
  TestTgtStreamPredictor = class(TTestCase)
  strict private
    FgtStreamPredictor: TgtStreamPredictor;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure TestLookChar;
    procedure TestReadChar;
  end;

implementation

procedure TestTgtStreamPredictor.SetUp;
begin
  FgtStreamPredictor := TgtStreamPredictor.Create(nil, 0, 0, 0, 0);
end;

procedure TestTgtStreamPredictor.TearDown;
begin
  FgtStreamPredictor.Free;
  FgtStreamPredictor := nil;
end;

procedure TestTgtStreamPredictor.TestLookChar;
var
  ReturnValue: Integer;
begin
  ReturnValue := FgtStreamPredictor.LookChar;
  // TODO: Validate method results
end;

procedure TestTgtStreamPredictor.TestReadChar;
var
  ReturnValue: Integer;
begin
  ReturnValue := FgtStreamPredictor.ReadChar;
  // TODO: Validate method results
end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTgtStreamPredictor.Suite);
end.

