// project-common-defines.mpc

project {
	macros += _GCD_STATIC

	macros += INSTLL_CIB
	macros += MULTI_INDEX_STREAM_FILE
	macros += NO_GSSCREEN
	macros += ONLY_NORM_INDEX
	macros += STORE_FORM_N
	macros += UPDATE_FACE
	macros += USE_CPP_ZIP
	macros += WINENCODE
	macros += GDS_NLOG_TRACE

	macros += VERSION56
	macros += VERSION61
	macros += VERSION62
	macros += VERSION63
}
