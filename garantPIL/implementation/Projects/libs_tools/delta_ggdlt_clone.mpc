project (delta_ggdlt_clone) : project-common-defines-tools, project-common-includes, project_generic_core, delta_base {
	staticname = delta_ggdlt_clone
	sharedname = delta_ggdlt_clone

	macros += MULTI_INDEX_STREAM_FILE
	macros += NEMESIS
	macros += VERSION63
	macros -= UPDATE_FACE
}
