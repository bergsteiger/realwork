project (delta_ggrev) : project-common-defines-tools, project-common-includes, project_generic_core, delta_base {
	staticname = delta_ggrev
	sharedname = delta_ggrev

	macros += MULTI_INDEX_STREAM_FILE
	macros += NEMESIS
	macros += VERSION63

	macros -= UPDATE_FACE
	macros += MIN_MEM_CONTEXT
}
