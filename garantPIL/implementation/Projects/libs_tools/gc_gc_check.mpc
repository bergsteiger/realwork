project (gc_gc_check) : project-common-defines-tools, project-common-includes, project_generic_core, gc_base {
	staticname = gc_gc_check
	sharedname = gc_gc_check

	macros += OPT_PACK_MPCXC
	macros += MULTI_INDEX_STREAM_FILE
}
