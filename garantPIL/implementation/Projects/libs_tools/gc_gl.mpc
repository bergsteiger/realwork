project (gc_gl) : project-common-defines-tools, project-common-includes, project_generic_core, gc_base {
	staticname = gc_gl
	sharedname = gc_gl

	macros += MULTI_INDEX_STREAM_FILE
	macros += MIN_MEM_CONTEXT
}
