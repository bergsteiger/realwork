project (gc_gsplit) : project-common-defines-tools, project-common-includes, project_generic_core, gc_base {
	staticname = gc_gsplit
	sharedname = gc_gsplit

	macros += OPT_PACK_MPCXC
	macros += MULTI_INDEX_STREAM_FILE
	macros += STORE_FORM_N
	macros += BLOCKSINHERIT
	macros += ONLY_NORM_INDEX
	macros += QUICK_NEW_DOC
	macros += MIN_MEM_CONTEXT
}
