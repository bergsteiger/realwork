project (gkdb_gpack) : project-common-defines-tools, project-common-includes, project_generic_core, gkdb_base {
	staticname = gkdb_gpack
	sharedname = gkdb_gpack

	macros += OPT_PACK_MPCXC
	macros += MULTI_INDEX_STREAM_FILE
	macros += STORE_FORM_N
	macros += QUICK_NEW_DOC
	macros += ONLY_NORM_INDEX
	macros += BLOCKSINHERIT
	macros += NEMESIS

	macros -= UPDATE_FACE
}
