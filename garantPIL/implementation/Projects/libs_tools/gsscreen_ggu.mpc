project (gsscreen_ggu) : project-common-defines-tools, project-common-includes, project_generic_core, gsscreen_base {
	staticname = gsscreen_ggu
	sharedname = gsscreen_ggu

	macros += MULTI_INDEX_STREAM_FILE
	macros += QUICK_NEW_DOC
	macros += STORE_FORM_N
	macros += ONLY_NORM_INDEX
	macros += BLOCKSINHERIT
	macros += NEMESIS
	macros += VERSION63
	macros += MIN_MEM_CONTEXT

	macros -= UPDATE_FACE

//        specific(make) {
                macros += _USE_STDOUT
//        }

}
