project (gsscreen_gpack) : project-common-defines-tools, project-common-includes, project_generic_core, gsscreen_base {
	staticname = gsscreen_gpack
	sharedname = gsscreen_gpack

	macros += OPT_PACK_MPCXC
	macros += MULTI_INDEX_STREAM_FILE
	macros += STORE_FORM_N
	macros += QUICK_NEW_DOC
	macros += ONLY_NORM_INDEX
	macros += BLOCKSINHERIT
	macros += NEMESIS

	macros -= UPDATE_FACE

//	specific(make) {
		macros += _USE_STDOUT
//	}

}
