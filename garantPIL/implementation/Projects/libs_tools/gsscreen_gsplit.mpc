project (gsscreen_gsplit) : project-common-defines-tools, project-common-includes, project_generic_core, gsscreen_base {
	staticname = gsscreen_gsplit
	sharedname = gsscreen_gsplit

	macros += OPT_PACK_MPCXC
	macros += MULTI_INDEX_STREAM_FILE
	macros += STORE_FORM_N
	macros += BLOCKSINHERIT
	macros += ONLY_NORM_INDEX
	macros += QUICK_NEW_DOC

	//specific(make) { // according to garant4x dsp settings - specific independent
		macros += _USE_STDOUT
	//}
}
