project (vcontrol_ggu) : project-common-defines-tools, project-common-includes, project_generic_core, vcontrol_base {
	staticname = vcontrol_ggu
	sharedname = vcontrol_ggu

	macros += MULTI_INDEX_STREAM_FILE
	macros += QUICK_NEW_DOC
	macros += STORE_FORM_N
	macros += ONLY_NORM_INDEX
	macros += BLOCKSINHERIT
	macros += NEMESIS
	macros += VERSION63
	macros += MIN_MEM_CONTEXT

	macros -= UPDATE_FACE
}
