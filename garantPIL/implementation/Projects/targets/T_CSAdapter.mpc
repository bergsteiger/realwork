project (T_CSAdapter) : project-common-defines, project-common-includes, project_generic_core, project_subsystem_console, Base_T_CSAdapter {
	sharedname = CSAdapter

	after += Core
	libs  += Core

	after += GCL
	libs  += GCL

	after += ContextSearch
	libs  += ContextSearch

	after += Morpho
	libs  += Morpho

	after += DBExt
	libs  += DBExt

	after += SearchAdapterLib
	libs  += SearchAdapterLib

	after += zlib
	libs  += zlib

}
