project (T_CmpEdit) : project-common-defines-tools, project-common-includes, project_generic_core+zlib-tools, project_subsystem_console, Base_T_CmpEdit {
	exename = cmpedit

	after += Core
	libs += Core
	after += GCL
	libs += GCL

	after += cchlp1
	after += cchlp2

	libs += cchlp1
	libs += cchlp2

	after += boost_thread
	libs  += boost_thread

	after += boost_system
	libs += boost_system
}
