project (T_Compiler) : project-common-defines-tools, project-common-includes, project_generic_core+zlib-tools, project_subsystem_console, Base_T_Compiler {
	exename = gc
	macros += MIN_MEM_CONTEXT

	after += Core
	libs  += Core

	after += GCL
	libs  += GCL

	after += endt
	libs  += endt

	after += evd
	libs  += evd

	after += gctx_tools
	libs  += gctx_tools

	after += gkdb_gudll
	libs  += gkdb_gudll

	after += cchlp2
	libs  += cchlp2

	after += vcontrol_ggu
	libs  += vcontrol_ggu
}