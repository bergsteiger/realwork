project (T_EntryPointAnalyzer) : project-common-defines-tools, project-common-includes, project_generic_core+zlib-tools, project_subsystem_console, Base_T_EntryPointAnalyzer {
	exename = EntryPointAnalyzer
	macros += MIN_MEM_CONTEXT

	after += gkdb_gudll
	libs  += gkdb_gudll

	after += cchlp1
	libs  += cchlp1

	after += gctx_tools
	libs  += gctx_tools

	after += Core
	libs += Core

	after += GCL
	libs += GCL

	after += Morpho
	libs += Morpho
}