project (T_MorphoAnalyzer) : project-common-defines-tools, project-common-includes, project_generic_core+zlib-tools, project_subsystem_console, Base_T_MorphoAnalyzer {
	exename = MorphoAnalyzer

	after += gkdb
	libs += gkdb

	after += SearchBase
	libs += SearchBase

	after += cchlp1
	libs += cchlp1

	after += gctx_tools
	libs += gctx_tools

	after += Core
	libs += Core

	after += GCL
	libs += GCL

	after += Morpho
	libs += Morpho
}