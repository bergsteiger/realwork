project (T_NormativeDefinitions) : project-common-defines-tools, project-common-includes, project_generic_core+zlib-tools, project_subsystem_console, Base_T_NormativeDefinitions {
	exename = NormDef
	macros += MIN_MEM_CONTEXT

	after += gkdb_gudll
	libs += gkdb_gudll

	after += cchlp1
	libs += cchlp1

	after += gctx_tools
	libs += gctx_tools

	after += endt
	libs += endt

	after += evd
	libs += evd

	after += Core
	libs += Core

	after += GCL
	libs += GCL
}